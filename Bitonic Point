class Solution{
public:
	
	int findMaximum(int arr[], int n) {
	    // code here
	    int max=arr[0];
	    for(int i=0;i<n;i++)
	    {
	        if(arr[i+1]<arr[i])
	        {
	            max=arr[i];
	            return max;
	            
	        }
	    }
	    
	}
};
